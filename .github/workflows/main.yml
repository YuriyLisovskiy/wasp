name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        sudo apt-get install libgtest-dev
        mkdir build
        cd build
        cmake ..
        make
        ./unit-tests
